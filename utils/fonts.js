import { Anton, Zilla_Slab } from 'next/font/google';

export const anton = Anton({ weight: ['400'], subsets: ['latin'] });
export const zillaSlab = Zilla_Slab({ weight: ['400'], subsets: ['latin'] })